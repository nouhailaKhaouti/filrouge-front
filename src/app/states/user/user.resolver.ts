import { Token } from '@angular/compiler';
import { ResolveFn } from '@angular/router';
import { User } from 'src/app/domain/user-model';

// export const userResolver: ResolveFn<Token> = (route, state) => {
//   return true;
// };
